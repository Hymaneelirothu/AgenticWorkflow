class PlanAgent:
    def split_task(self, query):
        """Splits the user query into smaller, manageable sub-tasks."""
        # Example: Split the query by logical steps
        return [
            f"Sub-task 1: Steps to publish a paper",
            f"Sub-task 2: Choose a journal",
            f"Sub-task 3: Prepare the manuscript"
        ]
